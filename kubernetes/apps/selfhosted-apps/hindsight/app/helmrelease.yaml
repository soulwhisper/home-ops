---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &name hindsight
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: gitops-system

  values:
    defaultPodOptions:
      automountServiceAccountToken: false
      hostUsers: false
    controllers:
      *name :
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          init-db:
            image:
              repository: ghcr.io/home-operations/postgres-init
              tag: "18"
            envFrom:
              - secretRef:
                  name: hindsight-pguser
        containers:
          app:
            image:
              repository: ghcr.io/vectorize-io/hindsight
              tag: 0.10.1-slim
            env:
              HINDSIGHT_API_OTEL_TRACES_ENABLED: "true"
              HINDSIGHT_API_OTEL_EXPORTER_OTLP_ENDPOINT: "http://opentelemetry-collector.monitoring-system.svc.cluster.local:4318"
              HINDSIGHT_API_VECTOR_EXTENSION: "vchord"
              HINDSIGHT_API_TEXT_SEARCH_EXTENSION: "pgroonga"
              # Embeddings via the siliconflow verification lane (/sf/*) while
              # MacStudio is offline — SAME model (Qwen/Qwen3-Embedding-4B,
              # 2560d), so existing pgvector data stays valid. Restore /v1 +
              # qwen3-embedding-4b when studio returns. Key from envFrom secret.
              HINDSIGHT_API_EMBEDDINGS_PROVIDER: "openai"
              HINDSIGHT_API_EMBEDDINGS_OPENAI_BASE_URL: "http://agentgateway-proxy.networking-system.svc.cluster.local:80/sf"
              HINDSIGHT_API_EMBEDDINGS_OPENAI_MODEL: "Qwen/Qwen3-Embedding-4B"
              # Rerank via the same lane (Qwen/Qwen3-Reranker-0.6B,
              # Cohere-compatible /sf/rerank).
              HINDSIGHT_API_RERANKER_PROVIDER: "cohere"
              HINDSIGHT_API_RERANKER_COHERE_BASE_URL: "http://agentgateway-proxy.networking-system.svc.cluster.local:80/sf/rerank"
              HINDSIGHT_API_RERANKER_COHERE_MODEL: "Qwen/Qwen3-Reranker-0.6B"
              HINDSIGHT_API_DATABASE_URL:
                valueFrom:
                  secretKeyRef:
                    name: hindsight-pguser
                    key: uri
            envFrom:
              - secretRef:
                  name: *name
            resources:
              requests:
                cpu: "200m"
                memory: 512Mi
              limits:
                cpu: "2"
                memory: 2Gi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities: { drop: ["ALL"] }
              readOnlyRootFilesystem: true

    service:
      app:
        ports:
          http:
            primary: true
            port: 9999
          api:
            port: 8888
    route:
      app:
        hostnames: ["memory.noirprime.com"]
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/icon: metabrainz.svg
          gethomepage.dev/name: Hindsight
          gethomepage.dev/group: Service
        parentRefs:
          - name: kgateway-internal
            namespace: networking-system

    persistence:
      tmpfs:
        type: emptyDir
        globalMounts:
          - path: /tmp
            subPath: tmp
