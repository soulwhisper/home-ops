---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: stirling-pdf
spec:
  refreshInterval: 12h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    template:
      data:
        SECURITY_INITIALLOGIN_USERNAME: "{{ .admin_user }}"
        SECURITY_INITIALLOGIN_PASSWORD: "{{ .admin_pass }}"
        SECURITY_OAUTH2_CLIENTID: "{{ .spdf_oidc_client_id }}"
        SECURITY_OAUTH2_CLIENTSECRET: "{{ .spdf_oidc_client_secret }}"
  dataFrom:
    - extract:
        key: app_user
    - extract:
        key: authentik
