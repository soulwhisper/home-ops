---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: netbox
spec:
  refreshInterval: 12h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    template:
      data:
        username: "{{ .admin_user }}"
        password: "{{ .admin_pass }}"
        email: "{{ .admin_email }}"
        api_token: "{{ .netbox }}"
        oidc_client_id: "{{ .netbox_oidc_client_id }}"
        oidc_client_secret: "{{ .netbox_oidc_client_secret }}"
        # netbox chart's YAML config loader globs /run/config/extra/*/*.yaml;
        # the configuration.py env mapping knows nothing of SOCIAL_AUTH_OIDC_KEY
        oidc-auth.yaml: |
          SOCIAL_AUTH_OIDC_KEY: "{{ .netbox_oidc_client_id }}"
          SOCIAL_AUTH_OIDC_SECRET: "{{ .netbox_oidc_client_secret }}"
  dataFrom:
    - extract:
        key: app-user
    - extract:
        key: encryption_cipher
    - extract:
        key: authentik
