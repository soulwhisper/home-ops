---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/external-secrets.io/clustersecretstore_v1.json
apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: kubernetes
spec:
  provider:
    kubernetes:
      # Source namespace for all secrets served by this store.
      remoteNamespace: monitoring-system
      server:
        caProvider:
          type: ConfigMap
          name: kube-root-ca.crt
          # everything for this store lives in monitoring-system: the secrets
          # (remoteNamespace), the CA ConfigMap, and the auth service account.
          namespace: monitoring-system
          key: ca.crt
      auth:
        serviceAccount:
          name: "eso-kubernetes"
          namespace: monitoring-system
