---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: authentik-blueprints-oidc-karakeep
spec:
  refreshInterval: 12h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  dataFrom:
    - extract:
        key: authentik
  target:
    template:
      data:
        karakeep.yaml: |
          # yaml-language-server: $schema=https://goauthentik.io/blueprints/schema.json
          version: 1
          context:
            client_id: "{{ .karakeep_oidc_client_id }}"
            client_secret: "{{ .karakeep_oidc_client_secret }}"
          entries:
            - model: authentik_providers_oauth2.oauth2provider
              id: karakeep-oidc-provider
              identifiers:
                name: &provider karakeep-oidc-provider
              attrs:
                name: *provider
                client_id: !Context client_id
                client_secret: !Context client_secret
                authorization_flow:
                  !Find [
                    authentik_flows.flow,
                    [slug, default-provider-authorization-implicit-consent],
                  ]
                invalidation_flow:
                  !Find [
                    authentik_flows.flow,
                    [slug, default-invalidation-flow],
                  ]
                client_type: confidential
                grant_types: ["authorization_code", "refresh_token"]
                redirect_uris:
                  - url: "https://bookmarks.noirprime.com/api/auth/callback/custom"
                    matching_mode: strict
                sub_mode: user_email
                include_claims_in_id_token: true
                signing_key:
                  !Find [
                    authentik_crypto.certificatekeypair,
                    [name, "authentik Self-signed Certificate"],
                  ]
                property_mappings:
                  - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
                  - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]
                  - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
                  - !Find [authentik_providers_oauth2.scopemapping, [scope_name, groups]]
            - model: authentik_core.application
              id: karakeep
              identifiers:
                slug: &name karakeep
              attrs:
                name: *name
                provider: !KeyOf karakeep-oidc-provider
                meta_launch_url: "https://bookmarks.noirprime.com"
                group: "Service"
                policy_engine_mode: all
            - model: authentik_policies.policybinding
              identifiers:
                target: !KeyOf karakeep
                group: !Find [authentik_core.group, [name, homelab-admin]]
                order: 0
              attrs: { enabled: true, timeout: 30 }
