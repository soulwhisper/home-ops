---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/gateway.networking.k8s.io/httproute_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: &name vmsingle-victoria-metrics-cluster
  annotations:
    gethomepage.dev/enabled: "true"
    gethomepage.dev/icon: victoriametrics.svg
    gethomepage.dev/name: VictoriaMetrics
    gethomepage.dev/group: Observability
spec:
  hostnames: ["metrics.noirprime.com"]
  parentRefs:
    - name: kgateway-internal
      namespace: networking-system

  rules:
    - matches:
        - path:
            type: Exact
            value: /
      filters:
        # SPA router expects its real base path; rewrite-at-/ renders blank
        - type: RequestRedirect
          requestRedirect:
            path:
              type: ReplaceFullPath
              replaceFullPath: /vmui/
            statusCode: 301
    - backendRefs:
        - name: *name
          port: 8428
---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/gateway.networking.k8s.io/httproute_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: &name vmalertmanager-victoria-metrics-cluster
  annotations:
    gethomepage.dev/enabled: "true"
    gethomepage.dev/icon: alertmanager.svg
    gethomepage.dev/name: AlertManager
    gethomepage.dev/group: Observability
spec:
  hostnames: ["alert.noirprime.com"]
  parentRefs:
    - name: kgateway-internal
      namespace: networking-system

  rules:
    - backendRefs:
        - name: *name
          port: 9093
