---
# yaml-language-server: $schema=https://soulwhisper.github.io/CRD-catalog/grafana.integreatly.org/grafanadashboard_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: database-cnpg
spec:
  folderRef: database
  resyncPeriod: 30m
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "victoria-metrics"
  configMapRef:
    name: dashboard-database-cnpg
    key: database-cnpg.json
---
# yaml-language-server: $schema=https://soulwhisper.github.io/CRD-catalog/grafana.integreatly.org/grafanadashboard_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: database-dragonfly
spec:
  folderRef: database
  resyncPeriod: 30m
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "victoria-metrics"
  configMapRef:
    name: dashboard-database-dragonfly
    key: database-dragonfly.json
