---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/grafana.integreatly.org/grafanadatasource_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDatasource
metadata:
  name: &name victoria-metrics
spec:
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasource:
    type: victoriametrics-metrics-datasource
    name: *name
    access: proxy
    isDefault: true
    url: http://vmsingle-victoria-metrics-cluster.monitoring-system.svc.cluster.local:8428
---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/grafana.integreatly.org/grafanadatasource_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDatasource
metadata:
  name: &name victoria-logs
spec:
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasource:
    type: victoriametrics-logs-datasource
    name: *name
    access: proxy
    url: http://victoria-logs-server.monitoring-system.svc.cluster.local:9428
---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/grafana.integreatly.org/grafanadatasource_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDatasource
metadata:
  name: &name victoria-traces
spec:
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasource:
    type: jaeger
    name: *name
    access: proxy
    url: http://victoria-traces.monitoring-system.svc.cluster.local:10428/select/jaeger
    jsonData:
      tracesToLogsV2:
        datasourceUid: victoria-logs
        spanStartTimeShift: -1m
        spanEndTimeShift: 1m
        tags: [{ key: "service.name", value: "service" }]
        filterByTraceID: true
        filterBySpanID: false
      tracesToMetrics:
        datasourceUid: victoria-metrics
        spanStartTimeShift: -2m
        spanEndTimeShift: 2m
      nodeGraph:
        enabled: true
      spanBar:
        type: Duration
---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/grafana.integreatly.org/grafanadatasource_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDatasource
metadata:
  name: &name alertmanager
spec:
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasource:
    type: alertmanager
    name: *name
    access: proxy
    url: http://vmalertmanager-victoria-metrics-cluster.monitoring-system.svc.cluster.local:9093
    jsonData:
      implementation: prometheus
---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/grafana.integreatly.org/grafanadatasource_v1beta1.json
# grafana.com dashboards type their datasource vars as `prometheus`; the
# victoriametrics plugin type can't match those vars. vmsingle speaks the
# Prometheus HTTP API, so expose it under that type. uid = legacy grafana.com
# "Prometheus default" uid used by hardcoded panels (ceph-osd, envoy-global).
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDatasource
metadata:
  name: prometheus
spec:
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasource:
    type: prometheus
    name: prometheus
    uid: PBFA97CFB590B2093
    access: proxy
    url: http://vmsingle-victoria-metrics-cluster.monitoring-system.svc.cluster.local:8428
