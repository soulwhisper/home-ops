---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # app
  # MacStudio offline — studio chat backends parked (see media/kustomization.yaml)
  # - ./agentgatewaybackend.yaml
  - ./agentgatewaypolicy.yaml
  # utils
  - ./externalsecret.yaml
  - ./guardrails.yaml
  - ./httproute.yaml
  # pre-mac verification lane
  - ./siliconflow.yaml
  - ./siliconflow-externalsecret.yaml
