---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # MacStudio offline — studio media backends (oMLX/vmlx/mlx_audio) parked;
  # consumers flipped to the /sf/* siliconflow lane. Restore both lines when
  # studio returns.
  # - ./agentgatewaybackend.yaml
  - ./httproute.yaml
