---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: hermes-agent
spec:
  refreshInterval: 12h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    template:
      data:
        # Web
        FIRECRAWL_API_URL: "http://firecrawl.selfhosted-apps.svc.cluster.local:3002"
        # LLM
        OPENAI_BASE_URL: "http://agentgateway-proxy.networking-system.svc.cluster.local:80/chat"
        OPENAI_API_KEY: "{{ .agentgateway_api_auth }}"
        # hermes gateway API server keys (>=16 chars; per-profile keys are scoped)
        API_SERVER_KEY: "{{ .api_server_key_root }}"
        CHAT_API_SERVER_KEY: "{{ .api_server_key_chat }}"
        # frigate-vision alert route HMAC secret (platforms.webhook, ops profile)
        FRIGATE_WEBHOOK_SECRET: "{{ .webhook_secret_frigate }}"
        # Feishu
        FEISHU_APP_ID: "{{ .homebot_app_id }}"
        FEISHU_APP_SECRET: "{{ .homebot_app_secret }}"
        FEISHU_VERIFICATION_TOKEN: "{{ .homebot_verify_token }}"
        FEISHU_ENCRYPT_KEY: "{{ .homebot_encrypt_key }}"
        FEISHU_ALLOWED_USERS: "{{ .homebot_users }}"
        FEISHU_HOME_CHANNEL: "{{ .homebot_channel }}"
  dataFrom:
    - extract:
        key: hermes-agent
    - extract:
        key: llm-api
    - extract:
        key: feishu
