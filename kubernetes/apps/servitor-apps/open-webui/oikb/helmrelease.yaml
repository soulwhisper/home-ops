---
# yaml-language-server: $schema=https://soulwhisper.github.io/k8s-schemas/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &name open-webui-oikb
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: gitops-system

  values:
    defaultPodOptions:
      automountServiceAccountToken: false
      hostUsers: false
    controllers:
      *name :
        annotations:
          reloader.stakater.com/auto: "true"
        # `oikb daemon` exits non-zero on sources: [] ("No sync entries
        # found") -> CrashLoopBackOff. Parked until the first Knowledge Base
        # source is configured in configmap.yaml; set replicas: 1 then.
        replicas: 0
        containers:
          app:
            image:
              repository: ghcr.io/open-webui/oikb
              # renovate: datasource=docker depName=ghcr.io/open-webui/oikb
              tag: v0.5.0
            args: ["daemon"]
            env:
              OPEN_WEBUI_URL: "http://open-webui:8080"
              LOG_FORMAT: "json"
            envFrom:
              - secretRef:
                  name: *name
            probes:
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet: { path: /health/ready, port: 8080 }
                  periodSeconds: 10
                  failureThreshold: 3
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet: { path: /health, port: 8080 }
                  periodSeconds: 30
                  failureThreshold: 3
            resources:
              requests:
                cpu: 10m
                memory: 128Mi
              limits:
                memory: 512Mi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities: { drop: ["ALL"] }
    service:
      app:
        ports:
          http:
            port: 8080
    persistence:
      config:
        type: configMap
        name: *name
        globalMounts:
          - path: /app/.oikb.yaml
            subPath: .oikb.yaml
            readOnly: true
