---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: servitor-apps
components:
  - ../../components/namespace
resources:
  # app
  - ./dify/ks.yaml
  - ./hermes-agent/ks.yaml
  - ./mcp-inspector/ks.yaml
  - ./open-notebook/ks.yaml
  - ./sillytavern/ks.yaml
  # mcp
  - ./mcp
  # : providers
  # search = selfhosted-apps/searxng
  # scraper
  - ./firecrawl/ks.yaml
  # memory
  - ./hindsight/ks.yaml
  - ./honcho/ks.yaml
  # local models
  - ./tei/ks.yaml
  # : observability
  - ./langfuse/ks.yaml
