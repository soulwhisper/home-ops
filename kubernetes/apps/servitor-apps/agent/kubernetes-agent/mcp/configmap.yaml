---
apiVersion: v1
kind: ConfigMap
metadata:
  name: kubernetes-agent
data:
  config.toml: |
    port = "8080"
    log_level = 2
    read_only = true
    stateless = true
    list_output = "yaml"
    toolsets = ["core", "config", "helm", "kubevirt"]
    [[denied_resources]]
    group = ""
    version = "v1"
    kind = "Secret"
    [[denied_resources]]
    group = "external-secrets.io"
    version = "v1"
    kind = "ExternalSecret"
