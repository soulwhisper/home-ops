---
# yaml-language-server: $schema=https://soulwhisper.github.io/CRD-catalog/objectbucket.io/objectbucketclaim_v1alpha1.json
apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
  name: ceph-bucket-langfuse
spec:
  bucketName: langfuse
  storageClassName: ceph-bucket
  additionalConfig:
    bucketOwner: "langfuse"
    bucketPolicy: |
      {
        "Version": "2012-10-17",
        "Statement": [
          {
            "Effect": "Allow",
            "Principal": {
              "AWS": "arn:aws:iam:::user/langfuse"
            },
            "Action": [
              "s3:*"
            ],
            "Resource": [
              "arn:aws:s3:::langfuse/*",
              "arn:aws:s3:::langfuse"
            ]
          }
        ]
      }
    # retentionPolicy 30d
    # bucketLifecycle: |
    #   {
    #     "Rules": [
    #       {
    #         "ID": "AbortIncompleteMultipartUploads",
    #         "Status": "Enabled",
    #         "Prefix": "",
    #         "AbortIncompleteMultipartUpload": {
    #           "DaysAfterInitiation": 1
    #         }
    #       }
    #     ]
    #   }
---
# yaml-language-server: $schema=https://soulwhisper.github.io/CRD-catalog/ceph.rook.io/cephobjectstoreuser_v1.json
apiVersion: ceph.rook.io/v1
kind: CephObjectStoreUser
metadata:
  name: langfuse
spec:
  store: ceph-objectstore
  clusterNamespace: storage-system
  keys:
    - accessKeyRef:
        name: rook-ceph-rgw-user
        key: LANGFUSE_ACCESS_KEY
      secretKeyRef:
        name: rook-ceph-rgw-user
        key: LANGFUSE_SECRET_KEY
